apiVersion: v2
name: oracle-cdc
description: Oracle CDC testing stack with OpenLogReplicator, Debezium, and Kafka
type: application
version: 0.2.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: "67.9.0"
    repository: https://prometheus-community.github.io/helm-charts
    condition: prometheus.enabled
